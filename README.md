# Travel way

## mail

If you want to use mail system, you have fill password in ```travel.ways.mail.password``` and change
value ```travel.ways.mail.send``` to `true`

## documentation

You can check API documentation on `http://localhost:8080/swagger-ui`. If you want to disable then
in `application.properties` set `springfox.documentation.enabled=false`.